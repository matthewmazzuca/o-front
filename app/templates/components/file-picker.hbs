<div class="setting">
	<div class="setting-title"> 		Photos 
		<br/>
		<img src="img/new-att-icon.png">
		<button class="button" {{action "pickWithFilepicker"}}>
			Add a photo
		</button>
	</div>
</div>

{{#if openPicker}}
  {{ember-filepicker pickerOptions=pickerOptions onSelection='fileSelected' multiple=multiple onClose='onClose' onError='onError'}}
{{/if}}